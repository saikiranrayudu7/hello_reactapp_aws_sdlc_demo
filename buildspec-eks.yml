version: 0.2

env:
  variables:
    REGION: us-east-1
    CLUSTER_NAME: devops-cluster
    REPOSITORY_URI: 779846799257.dkr.ecr.us-east-1.amazonaws.com/react-app-repo

phases:
  install:
    commands:
      - echo "Installing kubectl..."
      - curl -LO "https://amazon-eks.s3.us-west-2.amazonaws.com/1.27.0/2023-07-05/bin/linux/amd64/kubectl"
      - chmod +x kubectl
      - mv kubectl /usr/local/bin/kubectl

      - echo "Installing aws-iam-authenticator..."
      - curl -LO "https://amazon-eks.s3.us-west-2.amazonaws.com/1.27.0/2023-07-05/bin/linux/amd64/aws-iam-authenticator"
      - chmod +x aws-iam-authenticator
      - mv aws-iam-authenticator /usr/local/bin/aws-iam-authenticator

      - echo "Logging into Amazon ECR..."
      - aws ecr get-login-password --region $REGION | docker login --username AWS --password-stdin $REPOSITORY_URI

      - kubectl version --client  # Debug — remove after success

  pre_build:
    commands:
      - echo "Preparing build environment..."
      - IMAGE_TAG=$(echo $CODEBUILD_RESOLVED_SOURCE_VERSION | cut -c 1-7)
      - echo IMAGE_TAG=$IMAGE_TAG
      - export IMAGE_TAG

  build:
    commands:
      - echo "Building Docker image..."
      - docker build -t $REPOSITORY_URI:$IMAGE_TAG .
      - docker tag $REPOSITORY_URI:$IMAGE_TAG $REPOSITORY_URI:latest

  post_build:
    commands:
      - echo "Pushing Docker images to ECR..."
      - docker push $REPOSITORY_URI:$IMAGE_TAG
      - docker push $REPOSITORY_URI:latest

      - echo "Configuring kubectl for EKS..."
      - aws eks update-kubeconfig --name $CLUSTER_NAME --region $REGION

      - echo "Replacing image tag in manifests..."
      - sed -i "s|IMAGE_URI|$REPOSITORY_URI:$IMAGE_TAG|g" k8s/*.yaml

      - echo "Deploying to EKS..."
      - kubectl apply -f k8s/
