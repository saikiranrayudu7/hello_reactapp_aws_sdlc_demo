apiVersion: v1
kind: Service
metadata:
  name: react-frontend-service
  namespace: dev
  labels:
    app: react-frontend
spec:
  type: LoadBalancer          # 🔥 change this line
  selector:
    app: react-frontend
  ports:
    - port: 80
      targetPort: 80